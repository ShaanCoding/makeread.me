import { NextResponse } from "next/server"

/*
    Loads a specific macro
*/
export async function GET(request: Request) {
  return
}

/*
  const macros = await fetch(`/templates/${folderUrl}/macros.njk`).then((res) =>
    res.text()
  );
*/
